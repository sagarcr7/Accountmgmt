<%= simple_form_for Record.new do |record| %>
    <%= record.input :title %>
    <%= record.input :date %>
    <%= record.input :amount %>
     <%= record.submit 'Add Record', class: 'btn btn-primary' %>
   
<% end %> 